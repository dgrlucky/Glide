<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.dgrlucky.glide"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".AdvanceActivity"/>
        <activity android:name=".more.GlideActivity"
            android:theme="@style/AppCompatTheme"
            />

        <activity
            android:name=".more.image.SingleImageActivity"
            android:theme="@style/AppCompatFullscreenStyle"/>

        <activity
            android:name=".more.about.AboutActivity"
            android:theme="@style/AboutTheme"/>

        <activity android:name=".more.NineImageViewActivity"/>

        <activity android:name=".more.RecyclerViewActivity"/>

        <activity
            android:name=".more.image.ImagesActivity"
            android:theme="@style/ImagesStyle"/>
    </application>

</manifest>